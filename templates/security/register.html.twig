{#{% extends 'base.html.twig' %}

{% block body %}

<div class="container vh-100" style="margin-top:100px">
    <div class="rounded ">
        <div class="col-md-12 col-sm-12 shadow-lg p-5 bg-light mb-4">
        <h1 class="mt-4">Formulaire d'inscription</h1>
        {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="alert alert-{{ label }}">{{ message }}</div>
        {% endfor %}
        {% endfor %}

        {{ form(form) }}
        </div>
    </div>
</div>

{% endblock %}

{% block title %}
Inscription
{% endblock %}#}

{#{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
    <div class="container vh-100" style="margin-top:100px">
        <div class="rounded">
            <div class="col-md-12 col-sm-12 shadow-lg p-5 bg-light mb-4">
                <h1 class="mt-4">Formulaire d'inscription</h1>

                {% for label, messages in app.flashes %}
                    {% for message in messages %}
                        <div class="alert alert-{{ label }}">{{ message }}</div>
                    {% endfor %}
                {% endfor %}

                {{ form_start(form, {'action': path('app_security_registration'), 'method': 'POST'}) }}
                {% for field in form %}
                    <div class="mb-3">
                        {{ form_label(field) }}
                        {{ form_widget(field, {'attr': {'class': 'form-control'}}) }}
                        {% for error in field.vars.errors %}
                            <div class="text-danger">{{ error.message }}</div>
                        {% endfor %}
                    </div>
                {% endfor %}
                <button type="submit" class="btn btn-primary">Inscription</button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}

{% extends 'base.html.twig' %}

{% block body %}


{{ form_start(form) }}

{% for field in form %}
    {% if field.vars.name != '_token' and field.vars.name != 'submit' %}
        <div class="mb-3">
            {{ form_label(field) }}
            {{ form_widget(field, {'attr': {'class': 'form-control'}}) }}

            {% if field.vars.errors is defined and field.vars.errors|length > 0 %}
                {% for error in field.vars.errors %}
                    <div class="text-danger">{{ error.message }}</div>
                {% endfor %}
            {% endif %}
        </div>
    {% endif %}
{% endfor %}

{% if form.vars.errors|length > 0 %}
    <div class="alert alert-danger">
        {% for error in form.vars.errors %}
            {{ error.message }}
        {% endfor %}
    </div>
{% endif %}

<button type="submit" class="btn btn-primary">Inscription</button>

{{ form_end(form) }}

{% endblock %}#}

{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
<div class="container d-flex justify-content-center align-items-center" style="min-height: 100vh;">
    <div class="col-md-6 bg-light p-5 rounded shadow">
        <h2 class="mb-4 text-center">Formulaire d'inscription</h2>

        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-{{ label }}">{{ message }}</div>
            {% endfor %}
        {% endfor %}

        {{ form_start(form, {'action': path('app_security_registration'), 'method': 'POST'}) }}
            {% for field in form %}
                {% if field.vars.name != '_token' %}
                    <div class="mb-3">
                        {{ form_label(field) }}
                        {{ form_widget(field) }}
                        {% for error in field.vars.errors %}
                            <div class="text-danger">{{ error.message }}</div>
                        {% endfor %}
                    </div>
                {% endif %}
            {% endfor %}
            <button type="submit" class="btn btn-primary w-100">Inscription</button>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}
